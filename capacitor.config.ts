import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.qianhui.avatarcity',
  appName: 'Qian Hui Avatar City',
  webDir: 'dist',
}

export default config
