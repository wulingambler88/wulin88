# World UI guide

The persistent HUD is portrait-led and cream/pink with large touch targets. Navigation uses the existing SVG icon family through `WorldHUD`; shop catalogues collapse behind a contextual button so the physical place remains visible. Today’s optional panel contains three activities and a fixed 25-Star-Coin daily gift with no gambling or streak pressure.
